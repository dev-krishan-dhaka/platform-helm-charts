# Platform Helm Charts

Reusable Helm charts for all microservices.

## Charts Available
- backend-service — any Node.js/API backend
- frontend-service — any React/static frontend
- postgres — PostgreSQL with PVC

## Usage
These charts are consumed by gitops-environments repo via ArgoCD.
